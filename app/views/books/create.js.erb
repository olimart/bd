<% if @book.errors.any? %>
  $('#error-container').html("<%= j @book.errors.full_messages.to_sentence %>");
<% else %>
  $("#page-modal").modal('hide');
  $("#books").prepend('<%= j render(@book) %>');
  $(".alert-message").insert("All good");
<% end %>